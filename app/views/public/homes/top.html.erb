<main>
  <div class= "container">
    <div class= "row">
      <div class ="col-lg-12">
        <h1 class= "text-center">
          ようこそ、ながのcakeへ！<br>
          このサイトは、ケーキ販売のECサイトになります。<br>
          会員でない人でも商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。
        </h2>
        <div class= "col-lg-2">
          <h2 class= "table-secondary">新着商品</h2>
        </div>
        <div class= "row">
          <% @items.last(4).each do |item| %>
          <div class= "col-lg-2">
            <table class= "table-borderless">
              <tr>
                <th>
                  <%= link_to public_item_path(item.id) do %>
                    <% if item.image.attached? %>
                      <%= image_tag item.image, size: "200x200" %>
                    <% else %>
                      <%= image_tag 'no_image', size: "200x200" %>
                    <% end %>
                  <% end %>
                </th>
              </tr>
              <tr>
                <th>
                  <%= link_to public_item_path(item.id) do %>
                    <%= item.product_name %>
                  <% end %>
                </th>
              </tr>
              <tr>
                <th> ¥<%= item.price.to_s(:delimited) %></th>
              </tr>
            </table>
          </div>
          <% end %>
        </div>
        <div class= "col-lg-12">
          <div class= "pt-3"><%= link_to "すべての商品を見る＞", public_items_path %></div>

        </div>
      </div>
    </div>
  </div>
</main>